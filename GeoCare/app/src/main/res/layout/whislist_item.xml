<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="100dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/border_bottom_black"
    android:id="@+id/whistlist_item"
    android:backgroundTint="@color/blue_deep" android:padding="30dp">

    <ImageView
        android:id="@+id/whistlist_item_img"
        android:layout_width="60dp"
        android:layout_height="60dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"

        />

        <TextView
            android:id="@+id/whistlist_item_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toEndOf="@id/whistlist_item_img"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:text="Product name"
            android:fontFamily="@font/lato_medium"
            android:textColor="@color/blue_deep"
            android:textSize="16sp"
            />



</androidx.constraintlayout.widget.ConstraintLayout>